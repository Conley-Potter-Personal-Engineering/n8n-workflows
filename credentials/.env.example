# n8n API Configuration
N8N_API_KEY=your_api_key_here
N8N_BASE_URL=https://n8n.your-domain.com

# Optional: SSH Deployment Configuration (if using SSH fallback)
VPS_HOST=your-vps-ip
VPS_USER=root
# For local script use, you'd typically use your SSH agent, 
# but for GitHub Actions you'll need the SSH_KEY secret.
# VPS_SSH_KEY=
